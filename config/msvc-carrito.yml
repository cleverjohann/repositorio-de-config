server:
  port: 9033  # Puerto fijo para la aplicación
spring:
  application:
    name: msvc-carrito
  config:
    import: "classpath:/config/common.yml"

  jpa:
    hibernate:
      ddl-auto: update    
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/